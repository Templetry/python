# TemplateApp

Python CLI generated with [Templetry](https://github.com/Templetry): Typer commands, src layout, pytest with `CliRunner`.

```sh
pip install -e .[dev]
template-app --name you
pytest
```
